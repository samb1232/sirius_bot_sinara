import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#cf2d2a',
      light: '#cf2d2a',
      dark: '#cf2d2a',
     
    },
  },
});
export default theme;